#include<stdio.h>
main()
{
int n,i;
int kt=0;
printf("Nhap vai so can ktra:");
scanf("%d",&n);
if (n==1) printf ("k phai la so ngto");
for (i=2;i<n;i++)
  if (n%i==0) kt++;
if (kt==0) printf("So vua nhap la so ngto");
else printf("So vua nhap k phai la so ngto");
}